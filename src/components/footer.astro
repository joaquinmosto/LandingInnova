---
import { Icon } from "astro-icon/components";

const navLinks = [
  { label: "Inicio", url: "#" },
  { label: "Servicios", url: "#features" },
  { label: "Contacto", url: "#contact" },
  { label: "Proyectos", url: "#showcase" },
];

const socialLinks = [
  {
    url: "https://api.whatsapp.com/send/?phone=543515439227&text&type=phone_number&app_absent=0",
    description: "Contactar a Innova por WhatsApp",
    icon: "fa-brands:whatsapp",
    style: "bg-[#25D366] hover:bg-[#1eb954] text-white",
  },
  {
    url: "https://www.instagram.com/innova.webdev/",
    description: "Seguir a Innova en Instagram",
    icon: "fa-brands:instagram",
    style: "bg-gradient-to-tr from-[#f58529] via-[#dd2a7b] to-[#8134AF] text-white hover:opacity-90",
  },
];
---

<footer class="relative py-12 px-8 overflow-hidden">
  <div class="absolute inset-0 opacity-10 blur-2xl"
       style="background: radial-gradient(circle at center, rgba(255,255,255,0.1), transparent 70%);">
  </div>

  <div class="relative z-10 max-w-7xl mx-auto text-center space-y-14">
    
    <div class="flex justify-center">
      <img src="/isotipovioleta.WebP"
           alt="Innova logo"
           class="w-30 h-28 opacity-90 mix-blend-lighten" />
    </div>

    <div class="space-y-6">
      <h2 class="text-4xl sm:text-5xl font-bold tracking-tight leading-tight">
        Transformá tu presencia online.
      </h2>
      <p class="text-lg sm:text-xl text-gray-400 max-w-2xl mx-auto">
        Creamos sitios únicos, veloces y optimizados para tu negocio.
      </p>
      <a href="#contact"
         class="inline-block bg-[#5B13EC] hover:bg-[#4710b5] text-white font-semibold py-3 px-8 rounded-full shadow-md transition">
        Empezá tu proyecto
      </a>
    </div>

    <div class="flex justify-center gap-6">
      {socialLinks.map((link) => (
        <a href={link.url}
           target="_blank"
           rel="noopener noreferrer"
           aria-label={link.description}
           class={`flex items-center justify-center w-12 h-12 rounded-full transition ${link.style}`}>
          <Icon class="w-6 h-6" name={link.icon} />
        </a>
      ))}
    </div>
    
    <nav aria-label="Enlaces rápidos">
      <ul class="flex flex-nowrap justify-center gap-4 text-xs sm:text-sm font-medium">
        {navLinks.map((link) => (
          <li>
            <a href={link.url}
               class="relative inline-block px-2 py-1
                      transition duration-300 ease-out
                      hover:shadow-[0_0_8px_rgba(91,19,236,0.6)]
                      hover:backdrop-blur-sm
                      before:content-[''] before:absolute before:inset-0
                      before:border before:border-transparent before:rounded
                      hover:before:border-primary before:transition before:duration-300"
            >
              {link.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
    
    <div class="max-w-2xl mx-auto text-sm text-gray-400">
      <p>
        En Innova, diseñamos soluciones digitales rápidas, seguras y a medida. Elevamos tu marca con tecnología y creatividad.
      </p>
    </div>

    <div class="text-xs text-gray-500 mt-10">
      © {new Date().getFullYear()} Creado por nosotros, obvio — Innova, Córdoba, Argentina
    </div>

  </div>
</footer>
