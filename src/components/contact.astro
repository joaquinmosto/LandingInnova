---
import ContentSection from "~/components/content-section.astro";

const contactMethods = [
  {
    title: "Email",
    icon: "fa-solid:envelope",
    text: "innovainfo.io@gmail.com",
    url: "mailto:innovainfo.io@gmail.com",
  },
  {
    title: "Instagram",
    icon: "fa-brands:instagram",
    text: "@innova.webdev",
    url: "https://instagram.com/innova.webdev",
  },
  {
    title: "WhatsApp",
    icon: "fa-brands:whatsapp",
    text: "+5493515439227",
    url: "https://wa.me/543515439227",
  },
];
---

<ContentSection title="Contáctanos" id="contact">
  <Fragment slot="lead">
    ¿Tienes preguntas o necesitas más información?{" "}
    <span class="text-primary">¡Estamos aquí para ayudarte con tu proyecto!</span>
  </Fragment>

  <div class="relative w-full min-h-[80vh] flex items-center justify-center px-6 md:px-12">
    <h1
      class="absolute inset-0 font-bold opacity-60 tracking-wide text-transparent bg-clip-text bg-gradient-to-r from-[#404898] to-[#070019]/30 animate-typing overflow-hidden whitespace-nowrap"
      style="font-size: 23vw; top: 15%; left: 50%; transform: translateX(-50%); z-index: -1;"
    >
      INNOVA
    </h1>

    <div class="grid md:grid-cols-2 gap-12 w-full max-w-4xl relative">
      <div class="text-center md:text-left">
        <h3 class="text-xl font-extrabold mb-4">Charlemos</h3>
        <ul class="flex flex-col gap-4">
          {contactMethods.map((method) => (
            <li class="flex items-center gap-4">
              <i class={`text-2xl ${method.icon} text-primary`}></i>
              <a
                href={method.url}
                target="_blank"
                class="text-lg text-secondary hover:underline"
              >
                {method.text}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <form action="http://localhost:3000/enviar-contacto" method="POST" class="grid gap-6">
        <div class="flex gap-3">
          <input
            class="custom-input"
            type="text"
            placeholder="Nombre"
            id="name"
            name="name"
            required
          />
          <input
            class="custom-input"
            type="text"
            placeholder="Apellido"
            id="lastname"
            name="lastname"
          />
        </div>
        
        <input
          class="custom-input"
          type="email"
          placeholder="Email"
          id="email"
          name="email"
          required
        />
        <input
          class="custom-input"
          type="tel"
          placeholder="Teléfono"
          id="phone"
          name="phone"
          required
        />
        
        <textarea
          class="custom-input"
          placeholder="Escribe tu mensaje aquí..."
          id="message"
          name="message"
          rows="4"
          required
        ></textarea>
      
        <button
          class="w-full p-3 bg-[#6473d1] text-white font-bold hover:bg-[#404898] transition"
          type="submit"
        >
          Enviar
        </button>
      </form>
      
    </div>
  </div>
</ContentSection>

<style>
  @tailwind base;
  @tailwind components;
  @tailwind utilities;

  @keyframes typing {
    0% { width: 0; }
    50% { width: 100%; }
    70% { width: 100%; }
    100% { width: 0%; }
  }

  .animate-typing {
    animation: typing 8s steps(20, end) infinite alternate;
  }

.custom-input {
  background-color: rgba(255, 255, 255, 1);
  color: #000;
  padding: 12px;
  border: 1px solid #404898;
  border-radius: 8px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  transition: all 0.3s ease;
  width: 100%;
  box-sizing: border-box;
}

.custom-input:focus {
  border-color: #6473d1;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
  outline: none;
}

.form-row {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
}

.form-row > .custom-input {
  flex: 1 1 calc(50% - 12px);
}

@media (max-width: 768px) {
  .form-row > .custom-input {
    flex: 1 1 100%;
  }
}

.contact-card {
  background-color: rgba(255, 255, 255, 0.9);
  border-radius: 12px;
  padding: 16px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  transition: transform 0.2s, box-shadow 0.2s;
}

.contact-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
}

.contact-icon {
  background-color: #6473d1;
  color: #fff;
  border-radius: 50%;
  padding: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24px;
}

.contact-list {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

@media (min-width: 768px) {
  .contact-list {
    flex-direction: row;
  }
}
</style>
